'use strict';

var bind = require('function-bind');
var define = require('define-properties');

var implementation = require('string.prototype.trimright/implementation');
var getPolyfill = require('string.prototype.trimright/polyfill');
var shim = require('string.prototype.trimright/shim');

var bound = bind.call(Function.call, getPolyfill());

define(bound, {
	getPolyfill: getPolyfill,
	implementation: implementation,
	shim: shim
});

module.exports = bound;
