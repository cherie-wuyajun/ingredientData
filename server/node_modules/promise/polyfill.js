// not "use strict" so we can declare global "Promise"

var asap = require('asap');

if (typeof Promise === 'undefined') {
  Promise = require('promise/lib/core.js')
  require('promise/lib/es6-extensions.js')
}

require('promise/polyfill-done.js');
