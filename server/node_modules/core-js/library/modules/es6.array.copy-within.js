// 22.1.3.3 Array.prototype.copyWithin(target, start, end = this.length)
var $export = require('core-js/library/modules/_export');

$export($export.P, 'Array', { copyWithin: require('core-js/library/modules/_array-copy-within') });

require('core-js/library/modules/_add-to-unscopables')('copyWithin');
