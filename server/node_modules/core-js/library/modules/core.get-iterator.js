var anObject = require('core-js/library/modules/_an-object');
var get = require('core-js/library/modules/core.get-iterator-method');
module.exports = require('core-js/library/modules/_core').getIterator = function (it) {
  var iterFn = get(it);
  if (typeof iterFn != 'function') throw TypeError(it + ' is not iterable!');
  return anObject(iterFn.call(it));
};
