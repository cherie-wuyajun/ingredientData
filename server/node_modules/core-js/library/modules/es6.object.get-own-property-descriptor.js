// 19.1.2.6 Object.getOwnPropertyDescriptor(O, P)
var toIObject = require('core-js/library/modules/_to-iobject');
var $getOwnPropertyDescriptor = require('core-js/library/modules/_object-gopd').f;

require('core-js/library/modules/_object-sap')('getOwnPropertyDescriptor', function () {
  return function getOwnPropertyDescriptor(it, key) {
    return $getOwnPropertyDescriptor(toIObject(it), key);
  };
});
