require('core-js/library/modules/es6.regexp.search');
var SEARCH = require('core-js/library/modules/_wks')('search');
module.exports = function (it, str) {
  return RegExp.prototype[SEARCH].call(it, str);
};
