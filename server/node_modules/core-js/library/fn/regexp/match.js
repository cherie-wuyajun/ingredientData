require('core-js/library/modules/es6.regexp.match');
var MATCH = require('core-js/library/modules/_wks')('match');
module.exports = function (it, str) {
  return RegExp.prototype[MATCH].call(it, str);
};
