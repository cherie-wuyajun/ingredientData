'use strict';
var regexpExec = require('core-js/modules/_regexp-exec');
require('core-js/modules/_export')({
  target: 'RegExp',
  proto: true,
  forced: regexpExec !== /./.exec
}, {
  exec: regexpExec
});
